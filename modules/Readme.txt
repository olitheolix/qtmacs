Put auxiliary modules and packages in this directory.

Qtmacs will automatically the directories in this folder to the
Python search path (although not recursively). This means means that
all modules here can be directly imported in eg. applets.
